data:
  name: cad120
  path: ${oc.env:PWD}/data/CAD-120/features/preprocessed/cad120data.pickle
  path_zarr: ${oc.env:PWD}/data/CAD-120/features/faster_rcnn/features.zarr
  path_action_zarr: ${oc.env:PWD}/data/CAD-120/features/action_features.zarr
  path_caption_zarr: ${oc.env:PWD}/data/CAD-120/features/caption_features_ground_truth.zarr
  cross_validation_test_subject: Subject5 # Subject1, Subject3, Subject4, or Subject5
  video_id_to_subject_id: ${oc.env:PWD}/data/CAD-120/dictionaries/video-id_to_subject.txt
  scaling_strategy: null  # null or standard
  downsampling: 3  # 1 = full FPS, 2 = half FPS, ...
